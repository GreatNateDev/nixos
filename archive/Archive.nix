# winboat (System Package)
#  virtualisation.libvirtd = {
  #enable = ${ENABLE_LIBVIRTD};
  #qemu = {
   # package = pkgs.qemu_kvm;
    # };
#};

#services.opensnitch = {
  # enable = ${ENABLE_OPENSNITCH};
  #};
